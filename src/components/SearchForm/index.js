import React, { Component } from 'react'
//import Api from '../../api';

class SearchForm extends Component {
  render() {
    return(
      <div>Formulário</div>
    );
  }
}

export default SearchForm;
